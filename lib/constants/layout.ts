/**
 * Faixa única com o header: `mx-auto`, largura `calc(100% - 2rem)`, `max-w-lg`.
 * Sem padding horizontal no container; use **`py-*` / `pb-*`** nas páginas para espaço vertical.
 * Header: combine com `flex-row items-center justify-between gap-3`
 */
export const APP_SHELL_CONTAINER =
  "mx-auto flex w-[calc(100%-2rem)] max-w-lg";
