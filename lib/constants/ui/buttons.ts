/** Botão + no canto do título (Números, Categorias, Comandos) */
export const pageHeaderAddIconButtonProps = {
  size: "icon-lg" as const,
  variant: "default" as const,
  className: "shrink-0 touch-manipulation",
};

/** CTA em card vazio (“Cadastrar o primeiro”, etc.) */
export const emptyStateCtaButtonProps = {
  size: "sm" as const,
  variant: "default" as const,
};
